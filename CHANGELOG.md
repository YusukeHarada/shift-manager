# 変更履歴

このプロジェクトの主な変更点を記録します。
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

- **MAJOR** — 既存データの移行が必要になる変更（DB スキーマの破壊的変更など）
- **MINOR** — 後方互換のある機能追加
- **PATCH** — 後方互換のある不具合修正

## [1.1.0] - 2026-09-01

### 追加

- **半休オプション（AM休 / PM休）** — 通常勤務に組み合わせて半日休みを記録できるようになりました。「午前休みで午後から遅番」は `遅番` ＋ `AM休` のように入力します。早番・日勤など、どのベースシフトにも付けられます。
  - AM休 と PM休 は同時に成立しないため、片方を選ぶともう片方が自動的に外れます
  - 残業・会議・当直との併用は従来どおり可能です
  - iCal エクスポートでは `遅番（AM休）` の形式で出力されます

### 変更

- オプション選択ボタンを3列グリッド表示にし、選択肢が増えても各ボタンの幅が揃うようにしました

### 内部

- αオプションに `group` を指定すると同一グループ内で排他選択になる仕組み（`toggleAlphaKey()`）を追加し、シフト選択ポップアップと一括入力パネルで共通化しました
- テストを 48 件から 53 件に拡充しました

> **データ移行は不要です。** 既存の `shifts.alpha`（`text[]`）に保存キー `前休` / `後休` が増えるだけで、スキーマ変更はありません。

## [1.0.0] - 2026-07-30

初期リリース。タグを打っていなかったため、`c823065`（2026-05-22）から `952dfd3`（2026-07-30）までの累積を 1.0.0 として遡って記録します。

### シフト管理

- 月次シフトの手動入力（ベースシフト8種類 ＋ αオプション複数選択）
- カレンダー表示 / リスト表示の切り替え、月をまたいだナビゲーション（スワイプ対応）
- 複数日をまとめて入力できる入力モード
- シフト種別ごとの集計カード、次の出勤日の表示、勤務時間表

### データ・連携

- Supabase（PostgreSQL）によるデータ永続化と楽観的更新
- Supabase 認証によるメール／パスワードログイン、RLS 対応
- iCal（.ics）エクスポート（Googleカレンダー連携）

### UI

- CSS 変数によるデザインシステム、ダークモード対応
- フローティングボトムナビ、スケルトン表示、トースト通知
- テーマ切り替え（5種）と週の開始日設定
- フォントを Inter + Noto Sans JP に変更

### 修正

- 複数オプション指定時に iCal が重複イベントを作る不具合を修正
- ダークモードでシフト種別バッジの配色が崩れる問題を修正
- Android で再デプロイ後に白画面になる問題を修正

[1.1.0]: https://github.com/YusukeHarada/shift-manager/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/YusukeHarada/shift-manager/releases/tag/v1.0.0
