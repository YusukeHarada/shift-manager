# 変更履歴

このプロジェクトの主な変更点を記録します。
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

- **MAJOR** — 既存データの移行が必要になる変更（DB スキーマの破壊的変更など）
- **MINOR** — 後方互換のある機能追加
- **PATCH** — 後方互換のある不具合修正

> **1.1.0 より前のバージョンについて**
> このリポジトリはタグを作っていなかったため、`1.0.0` までは開発の経緯が分かるようコミット履歴から遡って区切ったものです。実際にその番号でリリースした記録はありません。各見出しの括弧内は、そのバージョンの最終コミットです。

## [1.3.0] - 2026-09-17

デザインの刷新（フェーズA）。**変更は `index.css` とシフト色の定数だけで、機能・画面構成・データ構造は変わりません。**

### 変更

- **枠線をやめ、塗りと余白と影で区切るようにしました。** カード・カレンダーのセル・リスト項目・バッジ・タブのすべてに 1px の枠線が付いていたのを撤去しました。枠線が残るのは入力欄と時間表の罫線だけです
- **バッジの色を整理しました。** 従来は1つのシフト色を「枠線・塗り・文字」の3箇所で使っていたため彩度が重なって騒がしくなっていました。「薄い塗り＋濃い文字」の2箇所だけに変更しています
- **文字サイズを整理しました。** 10px を全廃し、`--text-xs`（12px）〜`--text-2xl`（24px）のトークンに統一。ウェイトも 400 / 500 / 600 / 700 の4段階に整理しました（従来は 700 / 800 に偏って階層が出ていませんでした）
- **「今日」と「選択中」の表し方を変えました。** 塗り（今日）と外向きのリング（選択中）で形を分けたので、色が近くても混同しません
- **ビュー切り替えをセグメント型にしました。** 選択側が白く浮く形になります
- **ボトムナビの選択表示を、ボタン全体の塗りからアイコンを包むピルに変えました**
- **角丸を拡大し、影を接地影＋拡散影の2層にしました**
- **プライマリの色をワインレッド（`#7d3345`）から濃いインク（`#2f3640`）に変更しました。** シフト色が既に6色相を使っているため、UI の骨格を無彩色にして彩度をシフト色だけに集約しています。テーマ（オーシャン／フォレスト／サンセット／モノクローム）の色も彩度を一段落としました

### 修正

- **シフト色のコントラストを WCAG の 4.5:1 以上にしました。** 「休み」の `#94a3b8` は薄い背景の上で 2.1:1 しかなく、明るい場所では読めない状態でした。全10色の文字色を濃くしています（塗りの色は変えていません）
- **ダークモードでプライマリ上の文字が読めなかったのを修正しました。** プライマリが明色に反転するのに文字が `#fff` 固定だったためです（`--color-on-primary` を追加）
- **「休み」と「未入力」のセルが見分けられなかったのを修正しました。** 枠線を外したことで、中性色の淡い塗りが未入力セルの背景と同化していたため、塗りに差をつけています

### 内部

- 状態指定の `!important` を6箇所すべて解消しました（残るのは `prefers-reduced-motion` の4行のみ）
- `CLAUDE.md` に「デザインシステム」節を追加しました

> **データ移行は不要です。** DB スキーマ・アプリ内のデータ形式・コンポーネント構成はいずれも変わりません。テスト60件は無変更のまま通ります。

## [1.2.0] - 2026-09-17

### 修正

- **iCal エクスポートを再インポートしても予定が重複しなくなりました。** これまではシフトを変更してから書き出すと、Google カレンダー上に古い予定が残ったままになるケースがありました。
  - 原因は、イベントの UID にαオプションが含まれていたこと。`早番` を `早番＋残業` に変えると UID が変わり、Google カレンダー側は別の予定として扱うため、古い「早番」が消えずに残っていました
  - UID を日付だけで決めるようにしたので、同じ日のシフトをどう変更しても既存の予定が上書きされます
- **シフトを削除した日の予定がカレンダーから消えるようになりました。** 未入力の日も取り消し（`STATUS:CANCELLED`）として書き出すためです。
- 終日イベントの終了日が開始日と同じだったのを修正しました（iCalendar では終了日は排他的なので翌日を指す必要があります）。カレンダーアプリによって表示が崩れる可能性がありました。

### 変更

- **αオプションが複数ある日は1件の予定にまとまります。** 従来は `早番（残業）` と `早番（会議）` の2件に分かれていましたが、`早番（残業・会議）` の1件になります。

### 内部

- 各イベントに `DTSTAMP` と `SEQUENCE` を追加しました（`DTSTAMP` は iCalendar の必須項目、`SEQUENCE` は更新を古い情報として無視されないための連番）
- `SUMMARY` / `DESCRIPTION` の `,` `;` `\` をエスケープするようにしました
- テストを 53 件から 60 件に拡充しました

> **データ移行は不要です。** 変更は `ical.js` の書き出し処理のみで、DB スキーマとアプリ内のデータ形式は変わりません。
>
> **初回の再インポートについて。** 1.1.0 以前に書き出した .ics を登録している場合、α付きの日の古い予定（UID にαキーが含まれるもの）は今回の書き出しでは上書きできません。Google カレンダー側で手動削除するか、シフト専用カレンダーを作り直したうえで再インポートしてください。

## [1.1.0] - 2026-09-01 (`ae77c4e`)

### 追加

- **半休オプション（AM休 / PM休）** — 通常勤務に組み合わせて半日休みを記録できるようになりました。「午前休みで午後から遅番」は `遅番` ＋ `AM休` のように入力します。早番・日勤など、どのベースシフトにも付けられます。
  - AM休 と PM休 は同時に成立しないため、片方を選ぶともう片方が自動的に外れます
  - 残業・会議・当直との併用は従来どおり可能です
  - iCal エクスポートでは `遅番（AM休）` の形式で出力されます

### 変更

- オプション選択ボタンを3列グリッド表示にし、選択肢が増えても各ボタンの幅が揃うようにしました

### 内部

- αオプションに `group` を指定すると同一グループ内で排他選択になる仕組み（`toggleAlphaKey()`）を追加し、シフト選択ポップアップと一括入力パネルで共通化しました
- テストを 48 件から 53 件に拡充しました

> **データ移行は不要です。** 既存の `shifts.alpha`（`text[]`）に保存キー `前休` / `後休` が増えるだけで、スキーマ変更はありません。

## [1.0.0] - 2026-07-30 (`952dfd3`)

ここまでの機能一式で本番運用に入った状態を、安定版の区切りとして 1.0.0 とします。

### 変更

- フォントを Inter + Noto Sans JP に変更

## [0.10.1] - 2026-07-21 (`eb1335f`)

### 修正

- Android で再デプロイ後に白画面になる問題を修正。Service Worker がナビゲーションリクエストをキャッシュ優先で返し、古い `index.html` が存在しないアセットを参照していたため、ネットワーク優先＋オフライン時のみキャッシュへフォールバックする方式に変更

## [0.10.0] - 2026-07-09 (`800e81c`)

### 追加

- 設定パネルを新設し、**週の開始日**（月曜／日曜）を切り替え可能に（デフォルトは月曜始まり）
- **カラーテーマ5種**（デフォルト・オーシャン・フォレスト・サンセット・モノクローム）の切り替え
- 設定内容を localStorage に保存し、次回起動時も維持

### 変更

- テーマを背景色・文字色まで含むフルパレットに拡張

## [0.9.0] - 2026-07-05 (`05215b5`)

### 追加

- 読み込み中のスケルトン表示
- 保存成功時のトースト通知
- 月送りのスライドトランジションと、カレンダー／リスト領域の**スワイプによる月移動**
- エラーバナーの再読み込みボタン
- 集計カードの件数比率プログレスバー
- `:focus-visible` のアウトライン、`prefers-reduced-motion` 対応

### 変更

- アプリアイコン（favicon / apple-touch-icon）を新配色のフラットなカレンダーアイコンに刷新

## [0.8.0] - 2026-07-05 (`ca2b7a8`)

### 変更

- UI をバーガンディ基調の配色と SVG アイコンに刷新。グラデーションや絵文字アイコンを廃し、過剰な影を整理してフラットな階層に統一

### 修正

- ダークモードでシフト種別バッジの配色が崩れる問題を修正

## [0.7.0] - 2026-06-11 (`ab53224`)

### 追加

- **PWA 対応** — `manifest.json` と Service Worker を追加し、インストールとオフラインキャッシュに対応
- ヘッダーのボタンを**フローティングボトムナビ**に移動
- ダークモード（`prefers-color-scheme`）対応、Safe Area 対応
- ESLint / Prettier / テストカバレッジ（`@vitest/coverage-v8`）を導入

### 変更

- ヘッダーを1行にまとめ、カレンダーが1画面に収まるよう改善
- フォントサイズを 16px に変更（iOS の自動ズーム防止）、全ボタンのタップ領域を 44px 以上に確保

### 修正

- 一括保存が部分的に失敗したとき、UI がロールバックされない問題を修正（失敗した日付のみ戻す）
- iCal の `DESCRIPTION` に氏名がハードコードされていた問題を修正
- Supabase の環境変数が未設定のとき、分かりやすいエラーを出すよう変更

## [0.6.1] - 2026-06-09 (`556efb7`)

### 修正

- README に実際の Supabase URL が記載されていたためプレースホルダーに変更（セキュリティ）

### ドキュメント

- README にプロジェクト構成・テーブル作成 SQL・環境変数を追記、`CLAUDE.md` を新規作成
- `apple-touch-icon` の link タグを追加

## [0.6.0] - 2026-06-01 (`d1c7349`)

### 追加

- CSS 変数によるデザインシステムを導入（フレームワークなし）

### 変更

- ログイン画面とメイン画面（カレンダー・リスト）をモダンにリデザイン

## [0.5.0] - 2026-05-28 (`4c72a3e`)

### 追加

- **Supabase 認証によるログイン**（メール／パスワード）と RLS 対応

## [0.4.0] - 2026-05-24 (`4661606`)

### 追加

- **入力モード** — 複数の日付を選択してまとめてシフトを保存できる機能
- favicon / アプリアイコン

## [0.3.0] - 2026-05-23 (`740865e`)

### 追加

- シフト区分を**ベース勤務＋αオプション**の2階層に分割
- 日勤を追加
- **勤務時間表**（シフト区分ごとの開始・終了時刻）
- 集計カードに αオプションの件数を追加

### 変更

- 集計カードを画面下部に移動

### 修正

- αオプションを複数指定したとき、iCal に重複したイベントが作られる不具合を修正
- 当直の勤務時間を訂正

## [0.2.0] - 2026-05-23 (`f3f8d74`)

### 追加

- **Supabase（PostgreSQL）によるデータ永続化**
- **iCal（.ics）エクスポート** — Googleカレンダーへのインポートに対応

## [0.1.0] - 2026-05-22 (`c823065`)

初期実装。

### 追加

- 月次シフトの手動入力
- カレンダー表示 / リスト表示、月をまたいだナビゲーション
- シフト種別ごとの集計
- データ保存は localStorage（ブラウザ内のみ）
